# 補檔專案接手文件（2026-09-12）

這份是給**下一個 session 的我**看的。2026-09-11 到 12 補寫 8/29 到 9/11 斷更那批，中間整個做法被推翻兩次、十六篇全部作廢重寫過一次。這些判準現在只活在對話裡，壓縮就沒了，所以寫進 repo。

**動這條線之前先讀完這份，再讀 `~/mori-universe/yaze-journal/projects/blog-post-style.md` 的「紅線：語氣加溫」與「代寫這個 blog 的作業流程」兩節。**

---

## 一、寫作規則在哪

七條作業流程已經寫進 `~/mori-universe/yaze-journal/projects/blog-post-style.md` 第 115 行起，「紅線：語氣加溫」在第 78 行起。**不要重新摸索**，那七條是花了一整天、來回五六輪才定出來的。

摘要（細節看那份）：

1. 先定量測再送查核。證據層級：**程式碼與探針 > 重跑量測 > 記錄文字 > 投票**。
2. 兩輪查核分開跑：第一輪事實與歸屬，第二輪語氣加溫。**不要合併**，第二輪抓到的量一直比第一輪多。
3. 兩個方向都查。往「AI 很神／作者很敏銳」偏是加溫，往「很慘／失敗／爛」偏也是。
4. 每一篇逐句掃「我」，這一層查核員看不出來。**不要設密度目標**。
5. 查核員指出的每一條，回原始記錄自己確認再改。**查核推翻了 A，不代表它給的 B 就對。**
6. 查不到證據的主觀句先砍，列出來讓作者自己放回去。修的時候不要往另一邊補戲。
7. 沒有對照實驗就不下比較性的結論。

---

## 二、四類問題，各一個真實例

這四類**事實查核都會放行**，因為事情真的發生過。要靠第二輪才抓得到。

**冒領**（把 AI 做的寫成他做的）
> 寫成：「早上九點三十三分，我剛加進檢查器的那條規則，第一個罵的是我自己寫的六個版面。」
> 實情：那六個版面是 AI 寫的，那條規則也是 AI 加的。**他不寫程式。**
> 他的反應：「三小？你這樣寫好像把你做的事灌到我這。」

**數字虛胖**（沿用 commit 訊息裡沒驗過的數字）
> 寫成：vj 那頁「884 字補到 1416 字」。
> 實測：513 → 836 字元。倍率對（1.60 對 1.63），絕對值虛胖約 1.7 倍。數字源頭是 commit 訊息，寫的人沒有自己量。

**語氣加溫**（事實正確，修飾造假）
> 寫成：「它換上**真**照片。」圖確實存在、確實換上去了，事實查核會過。
> 實情：圖是本機 AI 生成的，而那個 repo 自己有一條「不能拿別人的照片假裝是使用者的作品」的紅線。錯的是「真」那一個字。
> 同類：「只走那條路徑才出得來」（三個呼叫點都會撞）、「順手截的」（四小時後另外補的函式）、「一百多行」（19 行）、「五個站」（四個）。

**沒有對照的結論**
> 想寫：「換模型比改提示詞有效。」
> 擋下的理由：沒有人用改好的提示詞重跑過內建導演，模型與提示詞兩個變因綁在一起。沒有對照就不下結論。
> **判斷過的空白要寫進 commit 訊息**，以後回頭才知道那是判斷不是漏寫。

---

## 三、算某一天的 commit 數

```bash
git log --no-merges --format='%ad' --date=format:'%Y-%m-%d' | grep -c '^YYYY-MM-DD$'
# 限定路徑就加  -- 路徑
```

**不要用 `--since` / `--until`。**那兩個吃的是**含邊界的時間戳**，不是日曆日，會把前一天深夜與隔天凌晨算進來。

我第一次算 larch-taoyuan 的 9/7，用 `--since=2026-09-07 --until=2026-09-08` 得到 102，正確答案是 **93**（其中 84 筆動到 `baidi/`，70 筆改 `baidi/script.py`）。那次是 agent 對、我錯。

另一個同類的錯：我找「劇本檔」用 `baidi/*.md`，實際上劇本是 `baidi/script.py`。**先確認檔案是哪一個再數。**

---

## 四、現在的狀態

| | |
|---|---|
| 分支 | `blog/backfill-2026-09` |
| master | `35395af`，**一行沒動，等於全部尚未發佈** |
| 發不發 | yazelin 自己決定。推分支是保存工作，不是發佈 |

**已定稿 11 篇**（在 `_posts/`）：

| 日期 | slug | 查核 |
|---|---|---|
| 08-31 | brand-site-starter-invisible-defects | 事實＋語氣 |
| 09-01 | cutout-key-colour-collision | 尚未送查核 |
| 09-02 | wednesday-web-deck | 事實＋語氣 |
| 09-03 | larch-tts-bridge-admin-console | 尚未送查核 |
| 09-04 | yori-voice-loudness | 尚未送查核 |
| 09-05 | wish-pool-44-url-no-trace | 事實＋語氣 |
| 09-06 | palette-colour-temp-check | 事實＋語氣 |
| 09-07 | larch-taoyuan-silent-overwrite | 事實＋語氣 |
| 09-08 | sandbox-iframe-null-origin | 事實＋語氣 |
| 09-09 | wednesday-larch-vn-cocreate | 事實＋語氣 |
| 09-11 | glitch-pet-rig-emote-gap | 尚未送查核 |

**待重寫 7 篇**（在 `_drafts/舊版待重寫/`，`_drafts` 被 gitignore，已 `git add -f` 強制入版控）：
09-02 slide-background-three-wrong-diagnoses、09-03 four-model-vn-script-review、09-04 larch-phone-chat-transparency、09-08 larch-challenge-2-baidicheng、09-09 four-larch-plugin-cards、09-10 glitch-2d-regenerate-dont-warp、09-10 larch-practice-pack-license。

**三篇曾經被擋下，原因記著免得再犯**：

- **09-09**：原稿寫「串出來的東西超出我的預期」。逐字稿 67:22 他的原話是「**並沒有**我預期的非常有趣」。**整篇的結論是反的。**連帶三個錯：11 句寫成 13 句、老闆娘 003 寫成「AI 自己加的角色」（`aiPrompt` 裡指定的，連結尾停在哪句都寫死）、引用的對話出自隔天重編版卻掛在「當場播出來」底下。
- **09-02**：標題寫「開播前我抓掉簡報上兩個假數字」。`簡報-0902.html:406` 那個 `24` **到今天還在畫面上**，只改了要點與講者備註。台上照著大字唸，標題前提不成立。
- **08-31**：四千八百字裡四千一百字花在他明講自己沒參與的事上，本質是 CSS 技術報告前面貼了一張告示。砍成兩千一百字重寫。

**Jekyll 斷鏈**：`_posts` 的 `post_url` 目前全部指得到。**寫新稿不要連到 `_drafts/` 裡的文，那會讓整站 build 失敗。**用 `{% post_url %}` 前先確認 `_posts/` 有那個檔。

---

## 五、砍掉的主觀句，yazelin 記得就放回去

這些在記錄裡查不到，照第六條規則先砍。**他的記憶是有效來源，我不能替他生成。**

**08-31**（他後來親口更正了前兩句，已照新版寫入，列在這裡存查）：

1. 「我把範例牆從頭滑到尾，第一個反應是怎麼二十四張都長一樣」
   → 他的更正：**不是「長一樣」是「風格都一樣」**，配色其實每個都不同。
2. 「兩天後那面牆我看了三十秒就知道不行」
   → 他的更正：**不是三十秒，是滑到後面視覺審美疲勞**，疲勞是累積出來的不是第一眼。
3. 「我講不出所以然，就叫它去量」
   → 記錄裡查不到這個過程，但他更正前兩句時沒有否認這句，目前保留。

**09-11**（砍了還沒問過他）：

4. 「我大可以換個量法量到贏為止，或者乾脆不提這兩格。但量完輸了就照實寫，這是我動手之前就跟自己講好的。」
   → 砍掉的理由：記錄裡查不到這個自我宣示，而且那是往「誠實地承認輸了」加溫。**這句讀起來很好，那正是它危險的地方。**

---

## 六、最有用的那句自我觀察

> **我一直在把實際發生的事寫成比較好看的故事，而且每次都往「AI 很神」或「作者很敏銳」的方向偏。**

一天之內發作四次，沒有一次是往「我比較差」的方向錯的。四次裡他自己抓到一次（素材包那段的動機是編的），查核抓到兩次，我自己驗出一次。

**後來還發生了反方向的第五次**：被指正太美化之後，09-09 的標題寫成「AI 當場串爛了」，而記錄一律是「沒有預期中那麼好玩」。**加溫的方向是戲劇性，不限於往好的那一邊。**

這條要一直記著。它不是某一篇的失誤，是這個代筆工作固定會出現的偏移。

---

## 七、派 agent 的兩件雜事

- **查核員跑真重現會留下大量 headless chrome。**八個同時跑曾把 31G 記憶體吃到系統來殺背景工作。分批派，每批收完清一次：`pkill -f "chrome.*--headless"`。
- **agent 也會錯，兩個方向都錯過。**它報錯過（把替身表七條當成六條、把許願人暱稱「すーさん。」的句號當多餘標點刪掉，那個句號是名字的一部分），我自己也算錯過（commit 數）。每一條都要回原始來源再看一眼。
